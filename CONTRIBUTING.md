# Contributing Guidelines

1. **Создайте ветку**: Не заливайте код напрямую в `main`. Используйте `git checkout -b имя-ветки`.
2. **Создайте Pull Request**: Сделайте запрос на слияние вашей ветки в `main`.
3. **Сообщайте об ошибках**: Если нашли баг в проекте, открывайте **Issues**.
